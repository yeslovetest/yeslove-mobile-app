import { StyleSheet } from "react-native"

const styles = StyleSheet.create({
  mediumContainer: {
    width: "30%",    
    aspectRatio: 1,   
    marginBottom: 8,
  },

  medium: {
    width: "100%",
    height: "100%",
    resizeMode: "cover",
    borderRadius: 8,
  }
})

export default styles
